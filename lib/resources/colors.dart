import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const Color light = Color.fromRGBO(0, 255, 0, 1.0);
  static const Color dark = Color.fromRGBO(0, 0, 139, 0.8);
  static const Color darkButton = Color.fromRGBO(72, 61, 139, 0.5);
  static const Color lightButton = Color.fromRGBO(240, 230, 140, 0.5);
}
